# ============================================================
# ATW — Anggaran Transparency Watch
# Python Dependencies
# ============================================================
# Install: pip install -r requirements.txt
# Python:  3.11+
# ============================================================

# ─────────────────────────────────────────────────────────────
# Web Framework
# ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.32.0
jinja2==3.1.4
python-multipart==0.0.12
pydantic==2.9.2
pydantic-settings==2.5.2

# ─────────────────────────────────────────────────────────────
# Database — PostgreSQL + PostGIS
# ─────────────────────────────────────────────────────────────
sqlalchemy==2.0.36
alembic==1.13.3
psycopg2-binary==2.9.10
geoalchemy2==0.15.2

# ─────────────────────────────────────────────────────────────
# Spatial / GIS
# ─────────────────────────────────────────────────────────────
geopandas==1.0.1
shapely==2.0.6
pyproj==3.6.1
fiona==1.10.1
rtree==1.3.0

# ─────────────────────────────────────────────────────────────
# Data Manipulation
# ─────────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4
openpyxl==3.1.5
pyarrow==17.0.0

# ─────────────────────────────────────────────────────────────
# Analytics / FVI — untuk modul internal
# ─────────────────────────────────────────────────────────────
scipy==1.14.1
scikit-learn==1.5.2
statsmodels==0.14.4
joblib==1.4.2
tqdm==4.66.5

# ─────────────────────────────────────────────────────────────
# Auth & Security
# ─────────────────────────────────────────────────────────────
# (hashlib & secrets sudah built-in di Python 3.11)
# email-validator==2.2.0   # Uncomment jika pakai EmailStr

# ─────────────────────────────────────────────────────────────
# Utilities
# ─────────────────────────────────────────────────────────────
python-dotenv==1.0.1
pyyaml==6.0.2
loguru==0.7.2

# ============================================================
# CATATAN PENTING
# ============================================================
# Package berikut di-INSTALL TERPISAH karena alasan kompatibilitas:
#
# ─── DuckDB (analitik kolumnar) ───
#   pip install duckdb==1.1.3
#
# ─── PyTorch (untuk LSTM/GRU) — pilih salah satu:
#   CPU:  pip install torch==2.4.1 torchvision torchaudio \
#           --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.1 (RTX 40-series):
#         pip install torch==2.4.1 torchvision torchaudio \
#           --index-url https://download.pytorch.org/whl/cu121
#
# ─── PyMC (Bayesian / Monte Carlo) ───
#   pip install pymc==5.16.2 arviz==0.19.0
#
# ─── ARIMA auto (pmdarima) ───
#   pip install pmdarima==2.0.4
#
# ─── PyQt6 (client internal) — install di environment terpisah ───
#   pip install PyQt6==6.7.1 PyQt6-WebEngine==6.7.0
#
# ─── Jupyter (untuk eksperimen FVI) ───
#   pip install jupyterlab ipykernel
# ============================================================

# ============================================================
# ENVIRONMENT RECOMMENDATION
# ============================================================
# Ada 3 environment conda untuk isolasi:
#
#   1. atw-core    — FastAPI + PostGIS + data science (yang ada di file ini)
#   2. atw-fvi     — Analytics lanjutan (PyTorch, PyMC, pmdarima)
#   3. atw-client  — PyQt6 desktop client
#
# Cara setup:
#   conda env create -f environment-core.yml
#   conda env create -f environment-fvi.yml
#   conda env create -f environment-client.yml
# ============================================================